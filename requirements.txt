# ── Core ─────────────────────────────────────────────────────────────
anthropic>=0.32.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── API server ────────────────────────────────────────────────────────
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0
requests>=2.31.0
starlette>=0.38.0

# ── Graph ─────────────────────────────────────────────────────────────
networkx>=3.3

# ── Orchestration (optional but preferred) ────────────────────────────
langgraph>=0.1.0

# ── Vector store (optional) ───────────────────────────────────────────
chromadb>=0.5.0

# ── Persistent store (optional) ───────────────────────────────────────
redis>=5.0.0

# ── Graph DB (optional) ───────────────────────────────────────────────
neo4j>=5.20.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# ── Phase 4: Observability ─────────────────────────────────────────────────────
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
prometheus-client>=0.20.0

# ── Phase 4: Ticket creation ───────────────────────────────────────────────────
# jira and servicenow are accessed via REST (requests already included)
